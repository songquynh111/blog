{
  "name": "blog",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "beautiful": "lint-staged",
    "start": "nodemon --inspect src/index.js --legacy-watch ",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch-scss": "sass --embed-source-map --watch src/resources/scss:src/public/css/",
    "prepare": "husky"
  },
  "lint-staged": {
    "src/**/*.{js,json,scss}": "prettier --single-quote --trailing-comma all --tab-width 4 --write",
    "*.{js,css,md}": "prettier --write"
  },
  "author": "dainv",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.1",
    "express-handlebars": "^8.0.1",
    "nodemon": "^3.1.7"
  },
  "keywords": [
    "f8blog"
  ],
  "description": "",
  "devDependencies": {
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "morgan": "^1.10.0",
    "prettier": "^3.3.3",
    "sass": "^1.80.3"
  }
}
